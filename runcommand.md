terminal 1 command to run frontend
cd health-chatbot-app

> > npm run dev

terminal 2 command to run backend
cd backend_fastapi
uvicorn main:app --reload --host 127.0.0.1 --port 8001

command to run project
